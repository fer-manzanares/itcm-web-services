spring.datasource.url= jdbc:sqlserver://127.0.0.1;databaseName=Ventas;encrypt=false
spring.datasource.username=UsuarioVentasDev
spring.datasource.password=12345

#HTTP enabled
http.port=8081

server.port=8443

# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/springboot.p12
# The password used to generate the certificate
server.ssl.key-store-password=itcm123
# The alias mapped to the certificate
server.ssl.key-alias=springboot

server.ssl.enabled=true